# safe-code v4.5 — Slim & Consistent Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Extract rare-path procedure detail from `skills/safe-code/SKILL.md` into four new Layer-3 references, deduplicate repeated prose, fix every cross-file contradiction from the 2026-07-15 audit, add Grounding Rules to the AGENTS.md template, and bump to v4.5.

**Architecture:** Pure documentation/script edits. No test runner: each task applies edits and verifies with `grep`/`wc`/`bash scripts/check-version.sh`. Extracted text moves **verbatim** (identified by exact heading boundaries, not line numbers, since lines shift). Every extraction leaves a 3-6 line inline summary holding the section's decision points + hard invariants, plus a `Layer 3 Trigger` line. The Six-File Save Rule and the Step 8 version banner stay fully inline.

**Tech Stack:** Markdown (Agent Skills format), Bash, `git`, `grep`.

**Source spec:** `docs/superpowers/specs/2026-07-15-v4.5-slim-and-consistent-design.md`

## Global Constraints

- Never remove a decision point or hard invariant from SKILL.md; move detail only.
- Extracted bodies are moved verbatim; only the new inline summaries are new prose.
- All commits local-only, conventional messages; never push.
- After the final task: `bash scripts/check-version.sh` exits 0 on `4.5`; `wc -l skills/safe-code/SKILL.md` <= 950.

---

## File Structure

| File | Change |
|---|---|
| `skills/senior-dev/SKILL.md` | Remove Deployment Domain Pattern section |
| `skills/codebase-pruner/SKILL.md` | Add under-safe-code draft-timing clause |
| `skills/safe-refactor-code/SKILL.md` | Add under-safe-code draft-timing clauses (Step 0.5, Step 5) |
| `skills/safe-code/references/save-procedure.md` | Create (extraction) |
| `skills/safe-code/references/legacy-migration.md` | Create (extraction) |
| `skills/safe-code/references/graph-integration.md` | Create (extraction) |
| `skills/safe-code/references/first-run.md` | Create (extraction) |
| `skills/safe-code/SKILL.md` | Extractions + compressions + version bump |
| `skills/safe-code/references/agents-md-authoring.md` | Add Grounding Rules section + preserve rule |
| `skills/safe-code/references/examples.md` | Example 4 rewrite, banner, contradiction fix |
| `README.md` | How It Works alignment, badge, What's New v4.5 |
| `CLAUDE.md` | Refresh stale claims |
| `scripts/check-version.sh` | Assert examples.md banner |

---

## Task 1: senior-dev — remove Deployment Domain Pattern

**Files:** Modify: `skills/senior-dev/SKILL.md`

- [ ] Delete the whole section from the heading `## Deployment Domain Pattern` up to (not including) `## Final Review Gate`.
- [ ] Verify: `grep -c "Deployment Domain\|domain.com" skills/senior-dev/SKILL.md` -> 0.
- [ ] Commit: `refactor(senior-dev): drop deployment-domain section (out of scope for a discipline skill)`

## Task 2: helper write-timing harmonization

**Files:** Modify: `skills/codebase-pruner/SKILL.md`, `skills/safe-refactor-code/SKILL.md`

- [ ] codebase-pruner, in Step 0 after the sentence ending `...do not rediscover the same uncertain candidates from scratch.` (first occurrence) append a new paragraph:

```
When running under safe-code, follow safe-code's Draft-Until-Save timing: draft these notes (and any `AGENTS.md` updates) in `.safe-code/SESSION.md` and let `/safe-code --save` apply them. Direct writes to `safe-refactor-code.md` and `AGENTS.md` are standalone behavior only.
```

- [ ] safe-refactor-code, in Step 0.5 immediately after the `> **Canonical authoring rules:** ...` blockquote, add:

```
> **Timing under safe-code:** when this skill runs under `/safe-code`, AGENTS.md *content* updates follow safe-code's Draft-Until-Save (draft in `.safe-code/SESSION.md`, apply on `/safe-code --save`; First-Run Population excepted). The mandatory-immediate write below applies when running standalone.
```

- [ ] safe-refactor-code, in `### 5. Sync Docs Before Finishing` after the first sentence, add: `When running under safe-code, draft these updates in `.safe-code/SESSION.md` instead and apply them on `/safe-code --save`.`
- [ ] Verify: `grep -c "under safe-code" skills/codebase-pruner/SKILL.md skills/safe-refactor-code/SKILL.md` -> >=1 each.
- [ ] Commit: `fix(helpers): follow safe-code draft-until-save timing when orchestrated`

## Task 3: extract `references/save-procedure.md`

**Files:** Create: `skills/safe-code/references/save-procedure.md`; Modify: `skills/safe-code/SKILL.md`

- [ ] Create the reference with header below, then move these SKILL.md bodies into it **verbatim** as sections: (1) Atomic Commit Split "Split procedure" numbered list + "Commit type mapping" table + fallback block; (2) both `## Last Session` block shapes; (3) the LOG.md Trim Rule procedure block; (4) the Step 7 Draft-Until-Save Sync Table (table only).

```md
# safe-code reference: --save procedure detail

> Loaded on demand by `/safe-code --save` (Layer 3). The binding rules — Six-File Save
> Rule, Draft-Until-Save, local-commit-only, never push — live inline in SKILL.md; this
> file holds the mechanical procedures and exact shapes.
```

- [ ] In SKILL.md replace `### Atomic Commit Split Rule` body (heading up to `### Six-File Save Rule`) with the summary + trigger (exact text in spec §3a / final file), keeping: grouping order, `docs: sync .safe-code session files` final commit, local-only never push, never `--no-verify`, no re-verification between commits, degrade-to-single-commit fallback with LOG note, "never fails or blocks".
- [ ] In SKILL.md replace the two Last Session code blocks under `### Last Session block (written by \`/safe-code --save\`)` with a 2-line pointer (fields list + reference).
- [ ] In SKILL.md replace `## LOG.md Trim Rule` body with: check on every save; >200 lines -> compress entries older than 7 days into `## Archived Summary` at the bottom; never delete information; pointer to reference.
- [ ] In SKILL.md replace the Step 7 sync table with a 3-line paragraph: draft in SESSION.md, apply on save, exceptions (scaffold, active specs, live issue entries), table lives in the reference, Six-File Save Rule stays binding.
- [ ] Verify: `grep -c "save-procedure.md" skills/safe-code/SKILL.md` -> >=3; `grep -c "Six-File Save Rule" skills/safe-code/SKILL.md` -> >=2; commit-type table exists only in the reference (`grep -c "dead-code removal, rename, restructure" skills/safe-code/SKILL.md` -> 0).

## Task 4: extract `references/legacy-migration.md`

**Files:** Create: `skills/safe-code/references/legacy-migration.md`; Modify: `skills/safe-code/SKILL.md`

- [ ] Create the reference (header analogous to Task 3, "Loaded when a legacy layout is detected") and move verbatim: the pre-v3/v3 layout block, migration steps 1-5, the content-mapping list, and the migration rules list.
- [ ] Replace the SKILL.md `### Legacy Layout Migration` body (heading up to `### Doc + Session Templates`) with the summary + trigger, keeping: detect on every command, scaffold-time (not save-time), `git mv` when tracked, never overwrite (report conflicts), never remove folders holding foreign files, one `decision` LOG entry, content rewrites drafted until save.
- [ ] Verify: `grep -c "legacy-migration.md" skills/safe-code/SKILL.md` -> >=1; `grep -c "Pre-v3 layout" skills/safe-code/SKILL.md` -> 0.

## Task 5: extract `references/graph-integration.md`

**Files:** Create: `skills/safe-code/references/graph-integration.md`; Modify: `skills/safe-code/SKILL.md`

- [ ] Create the reference ("Loaded on demand by Step 3f") and move verbatim: detection steps 1-4 including the `.mcp.json` JSON block, step 5 build sequence (three tool calls), steps 6-7 fallback/partial rules.
- [ ] Replace the SKILL.md `## Step 3f: Graph Readiness Check` body (heading up to `### 3f. Graph reasoning output`, which is deleted too — its fields fold into the summary) with the summary + trigger + one-line reasoning fields, keeping: accelerator-never-overrides-safety, bootstrap only project-local `.mcp.json` preserving existing servers, no global edits/installs, `Graph: unavailable` fallback, partial-coverage rule, "/safe-code owns helper orchestration".
- [ ] Verify: `grep -c "graph-integration.md" skills/safe-code/SKILL.md` -> >=1; `grep -c "mcpServers" skills/safe-code/SKILL.md` -> 0.

## Task 6: extract `references/first-run.md`

**Files:** Create: `skills/safe-code/references/first-run.md`; Modify: `skills/safe-code/SKILL.md`

- [ ] Create the reference ("Loaded on the first `/safe-code` run and when the Context Self-Test triggers") and move verbatim: the First-Run Population per-file table + its rules list; the Context Self-Test `### How` steps 1-4 including the Day-1 question table and adversarial grading; the `### Gaps are work, not just a score` detail.
- [ ] Replace the SKILL.md `### First-Run Population` body with the summary + trigger, keeping: first-run immediate write of evidence-derivable files (AGENTS.md, project-overview, architecture + Navigation map, code-standards, progress-tracker), conversation-only files stay template, never invent facts, revert to Draft-Until-Save once populated, then self-test.
- [ ] Replace the SKILL.md `## Context Self-Test` body with the summary + trigger, keeping: closed-book exam concept, when it runs, citation-or-fail rule, gaps-are-work routing (repo-discoverable vs Open Questions), `context_selftest:` stamp + summary reporting.
- [ ] Verify: `grep -c "first-run.md" skills/safe-code/SKILL.md` -> >=2; `grep -c "Should be answered by" skills/safe-code/SKILL.md` -> 0.

## Task 7: SKILL.md in-place compressions

**Files:** Modify: `skills/safe-code/SKILL.md`

- [ ] Insert `## Safety Invariants (every command, every mode)` section immediately after the Scope Rule code block: never push; never copy secrets/raw logs/stack traces/private URLs/`current-issues.md` content into committed files (sanitized LOG.md summary is the committed history); never overwrite existing files (create missing, append marked blocks, or report conflicts); never leave the project root.
- [ ] Delete the now-redundant repeats: the final paragraph of `### Draft-Until-Save Rule` (`The agent may append issue entries...never copy its raw content...`) becomes a pointer; the Issue Tracking `Safety:` list compresses to two lines pointing at Safety Invariants; the Step 7 trailing `Do not copy raw ... into any persistent file.` line is deleted.
- [ ] Doc Structure: merge the two bridge paragraphs (lines starting `\`/safe-code\` keeps all continuity...` and `Everything lives in one shared...`) into one short paragraph pointing at Provider Bridge (Step 1) for mechanics.
- [ ] Delete `## Deprecated Command Forms` (contradicts Command Recognition normalization).
- [ ] Step 2a: delete the duplicated `current-issues.md` paragraph (Loading Layers copy is canonical).
- [ ] Step 1 scaffold list: delete the four per-host bridge lines, keep the single `<current host's bridge only — ...>` line.
- [ ] Compress `### 3b. Detect remote platform` to ~10 lines: classification is informational; all three buckets -> local commit only; keep Bucket B deploy note, Bucket C note, "do NOT ask the user", "never causes an automatic push".
- [ ] Reasoning consolidation: under `### Reasoning Format` add `Steps 3-5 emit this same block with step-specific fields; do not invent a new shape.` Replace the 3c, 3e, 4b, and Step 5 pre-plan `Reasoning:` code blocks with one-line field lists (3f already folded in Task 5). Keep the Step 5 mode definitions (A/B/C bullets) and pre-plan check list intact.
- [ ] Feature Suggestion Rule: replace the 5-line lifecycle block with one line: `suggested -> approved -> in-progress -> done | rejected` plus the rejected-is-memory clause; drop the duplicate rejected bullet from Rules.
- [ ] Verify: `grep -c "Deprecated Command" skills/safe-code/SKILL.md` -> 0; `grep -c "Safety Invariants" skills/safe-code/SKILL.md` -> >=2; `grep -c "BUCKET A" skills/safe-code/SKILL.md` -> 0 (prose bullets replace the block); invariant greps of spec §7 all still hit.
- [ ] Commit (Tasks 3-7 together): `feat(safe-code): v4.5 slim-down — extract Layer-3 references, dedupe prose`

## Task 8: Grounding Rules in AGENTS.md template

**Files:** Modify: `skills/safe-code/references/agents-md-authoring.md`

- [ ] In the template, after the `## Session State` section and before `## Project Facts`, insert:

```md
## Grounding Rules (anti-hallucination)
- Answer project questions from `.safe-code/context/` files or the code itself; know which one you are citing.
- Before referencing a file, function, route, or command, verify it exists (open it or grep it).
- Context missing or unclear -> say so and record it in `.safe-code/context/progress-tracker.md` Open Questions; never fill gaps with generic training knowledge.
- Repo evidence beats any doc, including these context files; if they disagree, trust the repo and flag the mismatch.
- Never invent versions, dependencies, APIs, env vars, or team conventions you did not see in this repo.
```

- [ ] In the authoring rules ("Behaviour when AGENTS.md is missing vs existing", existing-file branch), add a bullet: `Preserve the Grounding Rules section; re-add it if a previous edit dropped it.`
- [ ] Verify: `grep -c "Grounding Rules" skills/safe-code/references/agents-md-authoring.md` -> 2.
- [ ] Commit: `feat(safe-code): add Grounding Rules to the AGENTS.md template`

## Task 9: examples.md fixes

**Files:** Modify: `skills/safe-code/references/examples.md`

- [ ] Example 4 ordered list, replace steps 7-9 with the atomic-split versions (split into atomic commits, code first then one final `docs:` commit; verify six files in the docs commit diff; report commit hashes + types).
- [ ] Example 4 sample close-out: banner -> `=== safe-code v4.5 session complete ===`; `Commit: a1b2c3d` -> `Commits: 2 atomic: refactor: remove dead legacyDate + old-uploader, docs: sync .safe-code session files`; add line `LOG entry plain: "Removed 2 unused files/functions; all tests pass."`.
- [ ] Replace the rule bullet `**\`current-issues.md\` is never written by the agent** and never committed — it is the user's local-only scratchpad.` with: `**\`current-issues.md\` is never committed.** The agent writes it only to append/update issue entries on an error trigger (Issue Tracking Rule) — and never copies its raw content into any committed file.`
- [ ] Verify: `grep -c "never written by the agent" skills/safe-code/references/examples.md` -> 0; `grep -c "v4.5 session complete" skills/safe-code/references/examples.md` -> 1.
- [ ] Commit: `fix(safe-code): update worked examples to v4.2+ save behavior; fix issue-tracker contradiction`

## Task 10: README fixes + What's New

**Files:** Modify: `README.md`

- [ ] Replace the `## How It Works` step list with one matching SKILL.md: Step 0 locate root; Step 1 scaffold + bridge + migrate legacy; Step 2 load layers + auto-resume; Step 3 git safety, run profile, graph readiness, helper routing; Step 4 dead-code audit (+4b config trust audit); Step 5 pre-plan check -> mode A/B/C; Step 6 execute removals slice-by-slice; Step 7 refactor + review + smoke-verify + draft docs; Step 8 final summary; `--save` applies docs + atomic local commits only.
- [ ] Badge -> `version-4.5`.
- [ ] Prepend a `**v4.5** — slim & consistent.` What's New entry: slimmer SKILL.md via four Layer-3 references (decision points inline, Six-File Save untouched); Grounding Rules in generated AGENTS.md; contradictions fixed (examples, README step map, deprecated-forms section removed); helper timing harmonized; check-version.sh covers examples banner.
- [ ] Verify: `grep -c "version-4.5" README.md` -> 1; `grep -c "v4.5" README.md` -> >=2.

## Task 11: check-version.sh — examples banner

**Files:** Modify: `scripts/check-version.sh`

- [ ] After the Step 8 banner check, add:

```bash
# ---- examples.md close-out banner(s) ----------------------------------------
EXAMPLES="$ROOT/skills/safe-code/references/examples.md"
if [ -f "$EXAMPLES" ]; then
	FOUND_ANY=0
	while IFS= read -r EBAN; do
		FOUND_ANY=1
		check "examples.md banner" "$EBAN"
	done < <(grep -oE 'safe-code v[0-9]+\.[0-9]+(\.[0-9]+)? session complete' "$EXAMPLES" | sed -E 's/^safe-code v//; s/ session complete$//')
	[ "$FOUND_ANY" -eq 0 ] && info "examples.md banner: not present (skipped)"
else
	info "examples.md: not found (skipped)"
fi
```

- [ ] Verify: `bash scripts/check-version.sh` still exits 0 (will only pass after Task 12's bump if examples banner already says 4.5 — run after Task 12).

## Task 12: CLAUDE.md refresh + version bump + final verification

**Files:** Modify: `CLAUDE.md`, `skills/safe-code/SKILL.md`

- [ ] CLAUDE.md: "nothing here is executed except two bash scripts" -> "except the bash scripts in `scripts/`"; version-places sentence -> frontmatter, Step 8 banner, README badge, README What's New, examples.md close-out banner (drop "both tutorials"); replace the "current in-flight v4.2 work" parenthetical with "(Since v4.2 the save splits into atomic conventional commits — still local-only.)"; "~1250 lines" -> "~950 lines"; add the four new reference files to the reference-ownership list.
- [ ] SKILL.md frontmatter `version: "4.4"` -> `"4.5"`; Step 8 banner `v4.4` -> `v4.5`.
- [ ] Run `bash scripts/check-version.sh` -> exit 0, all mentions 4.5 including examples.md banner.
- [ ] Run invariant greps (spec §7) -> all hit. Run `wc -l skills/safe-code/SKILL.md` -> <= 950.
- [ ] Read the final SKILL.md end-to-end; confirm every Layer 3 Trigger names an existing file (`ls skills/safe-code/references/`).
- [ ] Commits: `chore(scripts): check-version.sh asserts examples.md banner` then `docs: bump to v4.5 — README, CLAUDE.md, banner, badge`
